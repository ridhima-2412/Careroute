services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    ports:
      - "8000:8000"
    depends_on:
      - backend
