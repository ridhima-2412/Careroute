{
  "name": "smart-ambulance-frontend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "esbuild index.js --bundle --servedir=. --outfile=dist/bundle.js --loader:.js=jsx",
    "build": "esbuild index.js --bundle --outfile=dist/bundle.js --loader:.js=jsx"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "axios": "^1.7.9"
  },
  "devDependencies": {
    "esbuild": "^0.25.3"
  }
}
